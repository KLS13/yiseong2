const sumAndPrint = (a, b) => {
  const result = a + b;
  return `결과는 ${result}입니다.`;
};

const result = sumAndPrint(10, 20);
console.log(result);

const sumprint = (a,b,c) => {
  const total = a+b+c;
  return `결과는 ${total} 입니다.`;
}

console.log(sumprint(2,4,6));
