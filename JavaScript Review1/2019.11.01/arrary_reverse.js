const numbers = [1, 2, 3, 4];
numbers.reverse();

console.log('numbers:', numbers);

const stringArray = 'hello'.split('');
stringArray.reverse();
console.log('stringArray:', stringArray);

const name = [ 'java', 'c', 'db', 'js', 'html'];
name.reverse();

console.log(name);

const stringname = 'java'.split('');
stringname.reverse();
console.log(stringname);

if(stringname[1] === 'v') {
    console.log('v가 맞냐');
}