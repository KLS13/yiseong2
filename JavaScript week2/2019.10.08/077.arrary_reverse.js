const numbers = [1, 2, 3, 4];
numbers.reverse(); // 배열을 역순으로 만듬.

console.log('numbers:', numbers);

const stringArray = 'hello'.split('');
stringArray.reverse();
console.log('stringArray:', stringArray);
