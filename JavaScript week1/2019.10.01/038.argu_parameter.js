function printMessage(pMessage) {
    console.log(pMessage);
    }
    printMessage('hello');

const printKakao = (pKakao) => {
    console.log(pKakao);
}
    printKakao('Bye')

// parameter는 매개변수 : 함수가 외부로부터 값을 받을 때 사용하는 '변수'
// Argument는 인수 : 즉 함수로 넘기는 값 자체를 말함. printMessage('hello') 에서 hello 는 인자

